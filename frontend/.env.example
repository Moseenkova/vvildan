# Set this to "true" to use the backend's server-only DEV_CHAT_ID locally
VITE_DEV_ENV=true

# Specify the URL of your FastAPI backend (if running on a different domain or port)
VITE_API_URL=http://localhost:8000
